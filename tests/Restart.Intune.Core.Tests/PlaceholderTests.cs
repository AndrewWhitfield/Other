using Xunit;

namespace Restart.Intune.Core.Tests;

// Placeholder — real tests are added in the implementation phase.
public class PlaceholderTests
{
    [Fact]
    public void Core_assembly_loads() => Assert.True(true);
}
